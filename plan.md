# Plan

## 1. copyOtherFiles 配置没有生效

- [x] 完成

## 2. 智能 timeout max token

- [x] 完成

## 3. 翻译文件添加翻译元数据

- [x] 完成

翻译元数据包含翻译时的时间、用的 AI 模型、原文件内容的哈希值

## 4. 跳过未变化的翻译

- [x] 完成

如果翻译文件存在且原文件哈希值和翻译元数据中哈希值相同，可跳过翻译，可在配置文件中控制

## 5. 清理未记录的文件

- [x] 完成

翻译时记录翻译文件路径和复制的文件路径，翻译完成后删去输出目录没有记录的文件

## 6. 显示单文件翻译耗时

- [x] 完成

每次翻译一个文件后显示耗时和消耗的 tokens

## 7. 显示总翻译统计

- [x] 完成

全部翻译后显示文章数量、复制的文件、总耗时和消耗的 tokens

## 8. index.ts 换行符转换

- [x] 完成

读取 md 原文件时自动把 CR 换行符转化为 LF

## 9. 将 reasoning_content 纳入乱码和重复检测

- [x] 完成

## 10. 为 openai(ResponseData) 错误添加分级

- [x] 完成

可重试（非致命）：请求超时、响应乱码、重复内容、速率限制（429）
致命错误：DNS解析失败、API 连接失败，状态码：400 401 402 422 404 405 500

## 11. 翻译服务模块错误处理

- [x] 完成

收到致命错误应报错并退出，非致命错误重试至最大试错次数

## 12. 429 速率限制处理

- [x] 完成

收到数据限制 429 时默认等待10秒后重试，等待时间可在配置文件中设置

## 13. 达到最大试错次数时的行为配置

- [x] 完成

可选择跳过该文件继续下一个，或选择退出程序，可在配置中设置该行为

## 14. 跳过特定文件的翻译配置

- [x] 完成

在 translationConfig 中添加配置：
- 正则匹配 front-matter 某字段，有匹配则跳过
- 正则匹配 content，有匹配则跳过

## 15. 连续错误次数限制

- [x] 完成

连续翻译5个文件都失败时报错并退出，次数可在配置文件中设置

## 16. 改进占位符策略

- [x] 完成

专业术语(preservedTerms)使用"占位符(<TERM_word>)"替代，该占位符包含该单词(word)，可以让 AI 更好地在译文中合适的位置保留它
而保留字段(preservedFields) 仍然使用 占位符(<PTX_abc>)，但占位符长度尝试缩短

## 17. 使用 pino 日志

- [x] 完成

使用 pino 日志替换掉原有的日志服务，使用 pino-pretty 美化，可在配置文件中设置日志级别 是否输出到文件（没有美化的） 日志文件路径

## 18. 页眉页脚

- [ ] 完成

在翻译后的文件页头页尾添加段落，支持占位符: {model} {local} {targetLanguage} {sourceLanguage} {targetLang} {sourceLang}
占位符在翻译时替换，未填充的占位符保留原样

另外可单独设置 en 等翻译语言的页眉页脚，优先级更高

## 19. 智能分块

- [ ] 完成

当 Markdown 内容过长时，自动将其分割成多个小块进行翻译，保持 Markdown 结构完整性：
- 代码块不拆散
- 列表项不拆散
- 段落保持完整
- 分块后逐块翻译，最后合并

配置项：
- chunkSize: 单块最大 token 数（默认 1000）
- minChunkSize: 最小块 token 数

## 20. 翻译报告

- [ ] 完成

可在配置文件中开启并设置输出文件，包含：
- 配置摘要，翻译总耗时、总Tokens、文件数、总数、跳过数、成功数、复制文件数
- 每个文件的翻译信息：是否成功、hash、失败原因等